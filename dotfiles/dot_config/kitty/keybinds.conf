# Split horizontally (one above another)
map ctrl+shift+semicolon launch --cwd=current --location=hsplit

# Split vertically (side-by-side)
map ctrl+shift+backslash launch --cwd=current --location=vsplit

# Move between panes
map ctrl+shift+left neighboring_window left
map ctrl+shift+right neighboring_window right
map ctrl+shift+up neighboring_window up
map ctrl+shift+down neighboring_window down

# Resize panes
map ctrl+alt+left resize_window narrower
map ctrl+alt+right resize_window wider
map ctrl+alt+up resize_window taller
map ctrl+alt+down resize_window shorter
